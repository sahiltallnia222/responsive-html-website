To make this website we must know html css and javascript
if we want to set any data in the element then we have to use attribute data-name-nextname="value"
then to get this in the javascript we will use "p_btn.dataset.nameNextname"
